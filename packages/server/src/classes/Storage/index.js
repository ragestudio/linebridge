class Storage {
    constructor(params) {
        this.params = {...params}

        this.type = this.params.type
    }

    set = (key, value, options) => {

    }

    get = (key) => {

    }

    del = (key) => {

    }
}

module.exports = { Storage }