{
	"name": "shm_map",
	"version": "1.0.0",
	"scripts": {
		"install": "node-gyp configure && node-gyp rebuild",
		"build": "node-gyp build",
		"init-ide": "node-gyp configure -- -f compile_commands_json && cp build/Release/compile_commands.json . && node-gyp configure"
	},
	"gypfile": true,
	"dependencies": {
		"node-addon-api": "^8.8.0",
		"node-api-headers": "^1.9.0",
		"node-gyp": "^12.3.0"
	}
}
