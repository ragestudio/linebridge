{
  "name": "linebridge-monorepo",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@linebridge/engine-neo": "workspace:*"
  }
}
