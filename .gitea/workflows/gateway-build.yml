name: Build Gateway

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: true

      - name: Setup GoLang
        uses: actions/setup-go@v6
        with:
          go-version: 1.25

      - name: Make binaries
        run: make binaries
        working-directory: ./gateway

      - name: Upload artifacts
        uses: https://github.com/christopherHX/gitea-upload-artifact@v4
        with:
          name: ultragateway-binaries
          path: ./gateway/bin
